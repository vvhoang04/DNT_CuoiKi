import 'package:flutter/material.dart';

class AppPaddings {
  static const screen = EdgeInsets.symmetric(horizontal: 20);
  static const card = EdgeInsets.all(16);
  static const section = EdgeInsets.symmetric(vertical: 24, horizontal: 20);
}
