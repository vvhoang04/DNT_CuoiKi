import 'package:flutter/material.dart';

class AppColors {
  // --- 1. MÀU CHUNG (General) ---
  static const primary = Color(0xFF4B7BE5); // Xanh dương chủ đạo
  static const background = Colors.white;   // Nền trắng
  static const textPrimary = Color(0xFF1A1A1A); // Chữ chính
  static const textSecondary = Color(0xFF757575); // Chữ phụ
  static const inputBackground = Color(0xFFEBF1FA); // Nền ô nhập liệu
  static const inputHint = Color(0xFFA0A5BD); // Chữ gợi ý
  static const borderColor = Color(0xFFE0E0E0); // Viền mờ
  
  // --- 2. MÀU ONBOARDING ---
  static const onboardingPrimary = Color(0xFF6C63FF); // Tím đậm
  static const dotInactive = Color(0xFFE0E0E0); // Chấm tròn chưa chọn
  static const onboardingAccent = Color(0xFF8B85FF); // Tím nhạt (dự phòng)

  // --- 3. MÀU HOME SCREEN (Fix lỗi Member not found) ---
  
  // Nền thẻ tiến độ (Daily Progress)
  static const cardProgressBg = Color(0xFFF0F4FF); 
  
  // Vocabulary (Từ vựng) - Xanh dương
  static const vocabColor = Color(0xFF4B7BE5);     
  static const vocabBg = Color(0xFFE8F1FF);        
  
  // Grammar (Ngữ pháp) - Xanh lá
  static const grammarColor = Color(0xFF4CAF50);   
  static const grammarBg = Color(0xFFE8F5E9);      
  
  // Listening (Nghe) - Tím
  static const listeningColor = Color(0xFF9C27B0); 
  static const listeningBg = Color(0xFFF3E5F5);    
  
  // Speaking (Nói) - Hồng
  static const speakingColor = Color(0xFFE91E63);  
  static const speakingBg = Color(0xFFFCE4EC);     
  
  // Reading (Đọc) - Cam
  static const readingColor = Color(0xFFFF9800);   
  static const readingBg = Color(0xFFFFF3E0);      
  
  // Writing (Viết) - Vàng
  static const writingColor = Color(0xFFFFC107);   
  static const writingBg = Color(0xFFFFF8E1);      

  // Resources (Tài liệu) - Đỏ
  static const resourceColor = Color(0xFFF44336);  
  static const resourceBg = Color(0xFFFFEBEE);     
}